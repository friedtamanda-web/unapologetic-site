# The Unapologetic Leader — Deploy (flat, no new folders)
All pages and the 12 line-*.html pages + card-*.png images sit at the ROOT.
Upload: GitHub repo → Add file → Upload files → select ALL these files (the picker is fine; nothing is in a subfolder except the pre-existing img/). Keep CNAME. Commit to main.
Per-line URLs: https://theunapologeticleader.com/line-<slug>.html  (preview image: /card-<slug>.png)
